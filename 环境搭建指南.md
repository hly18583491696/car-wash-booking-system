# 汽车洗车预约系统 - 环境搭建指南

## 📋 环境搭建清单

### ✅ 第一步：Java开发环境

#### 1. 安装JDK 11
```bash
# 下载地址
https://www.oracle.com/java/technologies/javase/jdk11-archive-downloads.html
# 或者使用OpenJDK
https://adoptium.net/

# 验证安装
java -version
javac -version
```

#### 2. 配置环境变量
```bash
# Windows系统
JAVA_HOME=C:\Program Files\Java\jdk-11.0.x
PATH=%JAVA_HOME%\bin;%PATH%

# macOS/Linux系统
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk
export PATH=$JAVA_HOME/bin:$PATH
```

### ✅ 第二步：Node.js环境

#### 1. 安装Node.js 18.x
```bash
# 下载地址
https://nodejs.org/

# 验证安装
node --version  # 应该显示 v18.x.x
npm --version   # 应该显示 9.x.x
```

#### 2. 配置npm镜像（可选）
```bash
# 使用淘宝镜像加速
npm config set registry https://registry.npmmirror.com/

# 验证配置
npm config get registry
```

### ✅ 第三步：数据库环境

#### 1. 安装MySQL 8.0
```bash
# 下载地址
https://dev.mysql.com/downloads/mysql/

# 启动MySQL服务
# Windows: 在服务中启动MySQL80
# macOS: brew services start mysql
# Linux: systemctl start mysql
```

#### 2. 创建项目数据库
```sql
-- 连接MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE car_wash_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户（可选）
CREATE USER 'carwash'@'localhost' IDENTIFIED BY 'carwash123';
GRANT ALL PRIVILEGES ON car_wash_db.* TO 'carwash'@'localhost';
FLUSH PRIVILEGES;

-- 验证
USE car_wash_db;
SHOW TABLES;
```

#### 3. 安装Redis 6.2
```bash
# Windows
# 下载地址: https://github.com/tporadowski/redis/releases
# 解压后运行 redis-server.exe

# macOS
brew install redis
brew services start redis

# Linux (Ubuntu)
sudo apt update
sudo apt install redis-server
sudo systemctl start redis-server

# 验证Redis
redis-cli ping  # 应该返回 PONG
```

### ✅ 第四步：开发工具

#### 1. IntelliJ IDEA Ultimate
```bash
# 下载地址
https://www.jetbrains.com/idea/

# 必装插件
- Lombok Plugin
- MyBatis X
- Spring Boot Helper
- GitToolBox
```

#### 2. Visual Studio Code
```bash
# 下载地址
https://code.visualstudio.com/

# 必装插件
- Vue Language Features (Volar)
- TypeScript Vue Plugin (Volar)
- Auto Rename Tag
- Bracket Pair Colorizer
- ESLint
- Prettier
```

#### 3. 数据库管理工具
```bash
# Navicat Premium (推荐)
https://www.navicat.com/

# 或者免费的DBeaver
https://dbeaver.io/
```

### ✅ 第五步：版本控制

#### 1. 安装Git
```bash
# 下载地址
https://git-scm.com/

# 配置用户信息
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 验证
git --version
```

#### 2. 创建代码仓库
```bash
# 在GitHub或Gitee创建仓库
# 仓库名: car-wash-system
```

## 🚀 环境验证

### 验证脚本
创建一个验证脚本来检查所有环境是否正确安装：

```bash
# 创建 check-env.bat (Windows) 或 check-env.sh (macOS/Linux)

echo "=== 环境检查开始 ==="

echo "1. 检查Java环境..."
java -version
echo ""

echo "2. 检查Node.js环境..."
node --version
npm --version
echo ""

echo "3. 检查MySQL连接..."
mysql -u root -p -e "SELECT VERSION();"
echo ""

echo "4. 检查Redis连接..."
redis-cli ping
echo ""

echo "5. 检查Git..."
git --version
echo ""

echo "=== 环境检查完成 ==="
```

## 📁 项目目录结构准备

```
car-wash-system/
├── README.md
├── 系统开发计划书.md
├── 环境搭建指南.md
├── backend/                    # SpringBoot后端项目
│   ├── src/
│   ├── pom.xml
│   └── README.md
├── frontend/                   # Vue.js前端项目
│   ├── src/
│   ├── package.json
│   └── README.md
├── docs/                      # 项目文档
│   ├── api/                   # API文档
│   ├── database/              # 数据库文档
│   └── deployment/            # 部署文档
├── sql/                       # 数据库脚本
│   ├── init.sql              # 初始化脚本
│   └── test-data.sql         # 测试数据
└── docker/                    # Docker配置（可选）
    ├── docker-compose.yml
    └── Dockerfile
```

## ⚠️ 常见问题解决

### Java环境问题
```bash
# 问题：java命令找不到
# 解决：检查JAVA_HOME环境变量是否正确设置

# 问题：版本不对
# 解决：确保安装的是JDK 11，不是JRE
```

### Node.js问题
```bash
# 问题：npm安装慢
# 解决：使用淘宝镜像
npm config set registry https://registry.npmmirror.com/

# 问题：权限错误
# 解决：使用管理员权限运行命令提示符
```

### MySQL问题
```bash
# 问题：连接被拒绝
# 解决：检查MySQL服务是否启动

# 问题：密码错误
# 解决：重置root密码
```

### Redis问题
```bash
# 问题：连接失败
# 解决：检查Redis服务是否启动

# Windows下Redis启动
redis-server.exe redis.windows.conf
```

## 📝 环境搭建检查清单

- [ ] JDK 11 安装完成
- [ ] JAVA_HOME 环境变量配置
- [ ] Node.js 18.x 安装完成
- [ ] npm 可以正常使用
- [ ] MySQL 8.0 安装并启动
- [ ] 创建 car_wash_db 数据库
- [ ] Redis 6.2 安装并启动
- [ ] IntelliJ IDEA 安装并配置插件
- [ ] VSCode 安装并配置插件
- [ ] 数据库管理工具安装
- [ ] Git 安装并配置用户信息
- [ ] 创建项目代码仓库

## 🎯 下一步

环境搭建完成后，下一步将进行：
1. 创建SpringBoot项目
2. 创建Vue.js项目
3. 配置项目基础结构
4. 初始化Git仓库

完成环境搭建后，请运行验证脚本确保所有环境都正常工作！