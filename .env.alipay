# ================================
# Alipay MCP Server 配置文件
# ================================

# 必需配置项：
# 支付宝应用ID - 从支付宝开放平台获取 (参考 backend/src/main/resources/application-payment.yml)
AP_APP_ID=2021000000000000

# 支付宝应用私钥 - 由你生成的RSA私钥（去除头尾和换行符）
AP_APP_KEY=your-alipay-private-key-here

# 支付宝公钥 - 从支付宝开放平台获取的支付宝公钥（去除头尾和换行符）
AP_PUB_KEY=your-alipay-public-key-here

# 回调通知地址 - 支付宝异步通知你的服务器地址
AP_NOTIFY_URL=http://localhost:8080/api/payment/callback/alipay

# 支付成功跳转地址 - 用户支付后跳转的页面
AP_RETURN_URL=http://localhost:3000/payment/result

# ================================
# 可选配置项：
# ================================

# 运行环境 (production 或 sandbox)
# AP_CURRENT_ENV=production

# 加密算法 (RSA 或 RSA2)
# AP_ENCRYPTION_ALGO=RSA2

# 可用工具配置 (all 或指定工具名，用逗号分隔)
# AP_SELECT_TOOLS=all

# ================================
# 配置说明：
# 1. 所有配置项都需要去除换行符
# 2. 私钥和公钥需要去除 -----BEGIN ...----- 和 -----END ...----- 部分
# 3. 确保回调地址可以被外网访问（本地测试可使用内网地址）
# 4. 更多配置参考 backend/src/main/resources/application-payment.yml
# ================================